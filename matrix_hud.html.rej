--- matrix_hud.html
+++ matrix_hud.html
@@ -1141,11 +1141,13 @@
 }
 
 function updateActiveCount(){
-  document.getElementById('active-count').textContent=Object.values(statuses).filter(s=>s.status==='working').length+' ACTIVE';
+  const el=document.getElementById('active-count');
+  if(el) el.textContent=Object.values(statuses).filter(s=>s.status==='working').length+' ACTIVE';
 }
 
 function setOnline(){
-  document.getElementById('connection-status').textContent = 'ONLINE';
+  const el=document.getElementById('connection-status');
+  if(el) el.textContent = 'ONLINE';
 }
 
 // ===== INIT =====
